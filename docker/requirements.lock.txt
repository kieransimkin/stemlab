# Pinned top-level runtime dependencies for the StemLab container.
# torch/torchaudio are installed separately in Docker so a CUDA or CPU wheel
# index can be selected while preserving the captured version numbers.
beat-this==1.1.0
beatnet==1.1.1
bs-roformer==1.2.4
bs-roformer-infer==0.1.5
cmudict==1.1.3
cython==3.3.0
demucs==4.1.0
faster-whisper==1.2.1
librosa==0.11.0
madmom-prebuilt==0.17.post1
matplotlib==3.11.2
numpy==2.5.3
onnxruntime==1.30.0
openunmix==1.3.0
platformdirs==4.11.12
pyloudnorm==0.2.0
rich==15.0.0
scipy==1.17.1
soundfile==0.14.0
typer==0.27.2
# Sentence-transformers is installable on the Docker Python 3.13 runtime and
# powers opt-out lyric semantic embedding analysis. Model weights remain lazy.
sentence-transformers>=6.1,<7
# Functional structure + a fourth independent beat/downbeat route. Version 3.1
# declares Python >=3.9 and uses modern pure-PyTorch compatibility layers.
all-in-one-infer==3.1.0
# Web-service dependencies were added after the captured desktop snapshot.
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
python-socketio>=5.12,<6
